// Origen de una nota: cargada a mano en el panel o importada desde LinkedIn.
// Permite al panel/frontend distinguir y badgear las notas importadas.
export enum NewsSource {
    MANUAL = 'manual',
    LINKEDIN = 'linkedin',
}
